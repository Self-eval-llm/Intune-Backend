# Fine-tuning dependencies for Unsloth + LoRA
# Install with: pip install -r requirements_finetune.txt

# Core dependencies
torch
transformers
datasets
trl
peft
accelerate

# Unsloth (faster training)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Optional but recommended
bitsandbytes
sentencepiece
protobuf

# For better progress bars
tqdm
